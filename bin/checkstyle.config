### Ignore checking import statements because CheckStyle doesn't
### like the import xx.xx.* operator.
checkstyle.ignore.imports=true

### If tabs occur in a file, then count them as 4 spaces. This
### is used when checking the length of lines.
###
### NOTE: Be default tabs are not preferred in Java source.
checkstyle.tab.width=4

### No maximum line length
checkstyle.ignore.maxlinelen=^.*$

### Sets the maximum number of method parameters to 9.
checkstyle.maxparameters=9
